[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "autolabel-labeler-paddleocr"
version = "0.1.0"
description = "PaddleOCR labeler plugin for AutoLabelUi (detection + recognition)"
requires-python = ">=3.11"
license = { text = "AGPL-3.0-only" }
dependencies = [
    "autolabelui-sdk",
    "paddleocr>=3.0",
    "paddlepaddle>=3.0",
    "numpy>=1.26",
    "Pillow>=10.0",
]

[project.entry-points."autolabelui.labelers"]
paddleocr = "autolabel_labeler_paddleocr:PaddleOCRLabeler"

[tool.setuptools.packages.find]
include = ["autolabel_labeler_paddleocr*"]
