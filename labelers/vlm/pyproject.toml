[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nounbox-labeler-vlm"
version = "0.1.0"
description = "VLM labeler plugin for Nounbox (any OpenAI-compatible endpoint: Ollama, vLLM, cloud)"
requires-python = ">=3.11"
license = { text = "AGPL-3.0-only" }
dependencies = [
    "nounbox-sdk",
    "openai>=1.40",
    "Pillow>=10.0",
]

[project.entry-points."nounbox.labelers"]
vlm = "nounbox_labeler_vlm:VlmLabeler"

[tool.setuptools.packages.find]
include = ["nounbox_labeler_vlm*"]
